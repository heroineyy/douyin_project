apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: douyin-app-claim0
  name: douyin-app-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
